input {
  beats {
    port => 5044
  }
}

filter {
  if [message] =~ /^\s*\{/ {
    json {
      source => "message"
      tag_on_failure => ["_json_parse_failure"]
    }
  }
}

output {
  elasticsearch {
    hosts => ["http://elasticsearch:9200"]
    index => "lms-logs-%{+YYYY.MM.dd}"
  }
}
